services:
  shiori-button:
    image: shiori-button:latest
    container_name: shiori-button
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./visitorCount.json:/app/visitorCount.json
    restart: always
